from django.apps import AppConfig
class MedicalVisitsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.medical_visits"
    verbose_name = "Visites medicales"
